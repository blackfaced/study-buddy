{
  "name": "study-buddy-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Study Buddy HTTP server - serves child web UI and handles camera frames",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "lint": "oxlint --config ../.oxlintrc.json src/",
    "test": "vitest run && node --test ../web/shared/*.test.js && node --test ../web/buddy/*.test.js && node --test ../web/games/candy-math-island/*.test.js && node --test ../web/write/*.test.js && node --test ../web/capture/*.test.js && node --test ../web/review/*.test.js && node --test ../bin/friday-weekend-reminder.test.js",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "multer": "^2.2.0",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/express": "^5.0.6",
    "@types/multer": "^2.2.0",
    "@types/node": "^26.1.1",
    "@types/supertest": "^6.0.2",
    "@vitest/coverage-v8": "^2.1.9",
    "oxlint": "^1.78.0",
    "oxlint-tsgolint": "^7.0.2001",
    "supertest": "^7.0.0",
    "tsx": "^4.23.0",
    "typescript": "^7.0.2",
    "vitest": "^2.1.9"
  }
}
